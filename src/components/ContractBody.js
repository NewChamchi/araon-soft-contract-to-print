import React from 'react';

import './Contract.css'
const ContractBody = ({ children }) => {
    return (
        <div className='ContractBody'>
            {children}
        </div>
    )
};

export default ContractBody;